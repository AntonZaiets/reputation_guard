/** Truncate review snippet in the table. */
export const TICKET_LIST_SNIPPET_LEN = 140;

/** Fixed locale so server and client produce identical strings (avoids hydration mismatch). */
export const TICKET_LIST_DISPLAY_DATE_LOCALE = "en-US";

import type { SxProps, Theme } from "@mui/material/styles";

export const draftCardRootSx: SxProps<Theme> = { borderRadius: 2 };

export const draftCardBodySx: SxProps<Theme> = {
  whiteSpace: "pre-wrap",
};
